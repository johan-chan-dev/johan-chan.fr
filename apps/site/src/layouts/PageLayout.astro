---
import Base from './Base.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import type { Lang } from '../i18n/ui';
interface Props {
  lang: Lang;
  current: 'home' | 'work' | 'journal' | 'about';
  title?: string;
  description?: string;
  ogImage?: string;
  width?: 'wide' | 'reading';
  hasTranslation?: boolean;
  canonical?: string;
}
const { lang, current, title, description, ogImage, width = 'wide', hasTranslation = true, canonical } = Astro.props;
---
<Base lang={lang} title={title} description={description} ogImage={ogImage} canonical={canonical}>
  <Header lang={lang} current={current} hasTranslation={hasTranslation} />
  <main class:list={["atl-container pb-16 md:pb-24", width === 'reading' && 'atl-rise']}>
    {width === 'reading' ? (
      <article class="mx-auto max-w-[680px] pt-8 md:pt-10">
        <slot />
      </article>
    ) : (
      <slot />
    )}
  </main>
  <Footer lang={lang} />
</Base>
